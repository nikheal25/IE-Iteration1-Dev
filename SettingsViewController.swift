//
//  SettingsViewController.swift
//  GreenFarmMonitor
//
//  Created by Hanlin Shen on 8/4/20.
//  Copyright © 2020 Nikhil Gholap. All rights reserved.
//

import UIKit

class SettingsViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }
    

    @IBAction func tempSwitch(_ sender: UISwitch) {
        if (sender.isOn == true)
        {
            
        }else
        {
            
        }
    }
    @IBAction func humSwitch(_ sender: UISwitch) {
        if (sender.isOn == true)
        {
            
        }else
        {
            
        }
    }
    @IBAction func lightSwitch(_ sender: UISwitch) {
        if (sender.isOn == true)
        {
            
        }else
        {
            
        }
        
        
        
    }
    
    @IBAction func phSwitch(_ sender: UISwitch) {
        if (sender.isOn == true)
               {
                   
               }else
               {
                   
               }
    }
    
    @IBAction func alertSwitch(_ sender: UISwitch) {
        if (sender.isOn == true)
                      {
                          
                      }else
                      {
                          
                      }
    }
    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
